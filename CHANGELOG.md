# 更新记录

本文件记录 `main` 分支在正式发布之间的重要变化。每个稳定版本的完整说明仍以 GitHub Release 为准。

## 开发中

- 暂无未发布改动。

## v2.1.0 - 2026-08-02

### 最终审计与文档治理

- 新增中文全面优化报告，集中记录代码清理、识别与分类、文件事务、WinUI 交互、性能、发行可信度、验证结果和仍需外部条件的事项。
- 补齐最新稳定版 `v2.0.2` 的仓库内发布说明归档，并让根目录 `UPDATE_NOTES.md` 与归档由测试锁定一致。
- 项目元数据测试新增本地 Markdown 链接检查，覆盖根文档、GitHub 社区文件、架构文档和 Windows 脚本说明，阻止断链或越界链接进入主分支。
- 复核 GitHub Ruleset、必需检查、Secret Scanning、Push Protection、Dependabot、Discussions、MIT 许可证和社区健康文件，当前无需变更仓库安全设置。

### 异常注入与边界验证

- 文件名解析新增固定随机种子的 Unicode 边界测试，覆盖中日英文字、组合字符、Windows 保留字符、不成对代理字符、方向覆盖符和控制字符；标题、查询与目录名保证可安全写入 UTF-8。
- 标题与目录名归一化会移除可能造成界面文字反向伪装或报告编码失败的不可见控制符，不改变正常中日英标题、作者、卷号和副标题规则。
- EPUB 读取新增随机字节、有效文件随机截断和成员解压上限测试；损坏压缩包继续按“无本地元数据”降级，不让单个异常文件中断整批扫描。
- 文件整理新增每个移动位置的故障矩阵，并覆盖“文件已经移动但系统调用随后报错”和“部分报告重写失败”；恢复日志仍能重建真实移动结果并完成撤销。
- C# 测试会启动真实 Python Sidecar、强制结束进程并验证重连、重新握手与 bootstrap，防止只在模拟状态下通过。
- Sidecar 协议新增连续畸形 JSON、非法类型和极端 Unicode 请求流测试；每次返回结构化错误，后续合法请求与优雅关闭仍可继续。

### 首次使用与紧凑窗口体验

- 首次启动新增一次性“先预览，再整理”引导，直接锚定目录选择入口；关闭后不再打扰，也可通过标题区帮助按钮随时重新查看。
- 文件详情重构为可复用控件，统一空白、加载、失败、重试、候选比较和手动修正状态；高频切换结果时不再重复播放进入动画。
- 宽窗口继续并排显示结果与详情，窄窗口改为同一详情控件的轻量侧栏，并支持点击外部区域收起，不再动态创建模态对话框。
- 极窄或低高度窗口新增工作区纵向滚动，1024×700 下也能到达筛选、结果表格和底部确认操作；恢复正常尺寸后自动回到顶部。
- 左侧四步流程提取为独立控件，主页面 XAML 进一步缩短；新增首次引导、窗口尺寸解析、自适应断点和组件契约测试。
- Windows 构建新增 1024×700 紧凑外观冒烟，按显示 DPI 换算测试尺寸，并在详情侧栏打开时验证浅色、深色、Acrylic、Mica 和实色切换。

### 安装包体积与依赖边界

- WinUI 项目改为直接引用 Base、Foundation、InteractiveExperiences 和 WinUI 四个 Windows App SDK 组件，不再通过完整元包携带未使用的 AI、Windows ML、Widgets 和 DWrite 运行文件。
- 同条件 `dotnet publish` 输出由 221.51 MiB 降到 169.92 MiB，减少 23.3%；包含 Python Sidecar 和许可证的完整暂存目录为 180.00 MiB。
- 实际 Inno Setup 安装器由 72,482,903 字节降到 56,360,775 字节，减少 22.2%，同时继续保持 .NET、Windows App SDK 和 Python 全自包含。
- 构建新增 210 MiB 暂存上限、可选组件文件拒绝清单、组件锁定测试和细化 SBOM/许可证校验，防止后续依赖升级静默恢复冗余运行时。

### 发布可信度

- Windows 构建现在随安装器生成 GNU 格式 `SHA256SUMS.txt`、SPDX 2.2 SBOM 和结构化构建信息，并在替换 `dist\winui` 前后分别验证资产集合与哈希；补充运行组件后的最终 SBOM 还会再次交给 Microsoft 工具解析。
- SBOM 使用固定版本的 Microsoft SBOM Tool 扫描实际安装器、WinUI/.NET 依赖和 PyInstaller 依赖，并补充内嵌 CPython 与 Inno Setup 组件；缺少关键运行组件会阻断构建。
- 新增严格的 Authenticode 可选入口：证书存在时签署项目自有二进制和最终安装器，强制 SHA-256、RFC 3161 时间戳、SignTool 与 PowerShell 双重验证；无证书时明确标记未签名。
- 构建信息记录源码提交、引用和工作树是否包含未提交修改，避免把本地脏构建误认为某个提交的精确产物。
- 新增标签发布工作流，版本、标签和中文说明一致后才构建 Release，并通过 GitHub OIDC/Sigstore 生成 SLSA 来源证明与 SBOM 证明。
- 发布资产验证器拒绝路径穿越、重复哈希项、未列入清单的文件、被篡改的安装器、不一致的版本和不完整 SBOM。

### 大型书库性能基线

- 新增完全使用合成文件的 10,000 文件递归扫描基准，覆盖取消响应、冷缓存扫描、热缓存重扫、缓存复用率和进程峰值工作集。
- Windows CI 现在执行可版本化的性能预算；预算保留较宽硬件余量，用于拦截数量级退化，不把共享 runner 的正常波动误判为失败。
- 文件名发布词清理改为预编译的合并正则，并用有界 LRU 消除同一文件计划内的重复解析；本地 10,000 文件热扫描由约 17.5 秒降至约 8.5 秒。
- 快速签名和完整哈希在同一只读句柄上建立初始安全快照，减少冷扫描重复打开文件，同时保留结束后的路径快照复核和完整 SHA-256 重复确认。
- WinUI 增加 10,000 行搜索、状态筛选和系列选项性能测试，防止结果区退化为平方级处理。

### 在线来源可靠性与控制

- Bangumi、AniList、Jikan 及显式注册的第三方来源现在拥有相互独立的请求间隔、失败冷却和健康统计；单一来源失败后会暂时跳过，不再对整批文件重复等待同一超时。
- 连续失败采用有上限的指数冷却，任一成功请求会重置连续失败计数；冷却只影响对应来源，后续来源与完整本地识别始终继续。
- 提供器正常返回“无结果”时写入仅驻留当前进程的短期负缓存；网络异常、契约错误和取消操作不会被误记为无结果。
- 设置页新增来源级启用开关、查询优先级和空闲、正常、降级、冷却、禁用状态；优先级或启用组合变化会隔离元数据缓存命名空间。
- 来源健康快照不保存查询内容，只公开有界错误、调用统计和剩余冷却时间；旧设置默认继续启用全部内置来源。

### Python 分类核心分层

- 原先超过千行的 `classification.py` 改为稳定兼容门面；文件发现、计划生成、安全校验、报告、恢复日志、移动执行和撤销分别进入独立模块。
- 历史 `lightnovel_selector.classification` 与 `lightnovel_classifier.py` 的公开函数导入路径保持不变，Sidecar 方法、报告结构和文件移动语义不变。
- 扫描缓存、取消检查和进度回调提取为不持有线程或 UI 状态的 `ScanSession`，应用服务只负责任务线程、日志和快照发布。
- 针对扫描会话补充独立测试，并继续由完整移动、部分失败、恢复日志和可重入撤销测试锁定磁盘事务行为。

### 识别校准与本地修正记忆

- 用户明确保存的单条或同系列修正会形成有界的本地系列别名字典；后续扫描精确命中时优先复用，并在详情中标记为“本地修正记忆”。
- 修正记忆只保存别名、确认后的系列名和更新时间，不保存小说路径或正文；损坏、被锁定或写入失败只会失去复用能力，不会阻断扫描和当前修正。
- 文件名解析新增中文数字、日文“巻”和规范罗马数字卷号，标题比较统一处理全半角、破折号、波浪号、引号和可选副标题。
- 作者名统一清理“作者/著者/Writer”等角色前后缀，并以忽略空格和标点的比较键去重，不改变首个可信来源的显示顺序。
- 不同来源的原始分数经过同一保守标尺校准；结果新增高、中、需复核等级，并展示规则、修正记忆、标题一致性、内容提示、卷号、作者和语言等分类依据。
- 新增不含用户路径、账号或正文的脱敏文件名与 EPUB 元数据语料，覆盖中、日、英命名结构及跨格式回归。

### 统一书籍身份

- 新增统一 `BookIdentity`，以同一数据结构承载书名、系列、作者、卷号、语言和标签。
- EPUB 本地读取标准标题、作者、语言和主题字段，并兼容 Calibre/EPUB 3 的系列与卷号元数据。
- TXT、HTML、Markdown 与文件名采用保守语言识别；证据不足时保持“未识别”，不根据短标题强行猜测。
- Bangumi、AniList 和 Jikan 的可用作者、类型标签与卷号信息统一映射到身份模型。
- 元数据缓存继续兼容旧条目；分类报告和 Sidecar 协议以附加字段提供完整身份，不破坏旧报告撤销。
- WinUI 详情区显示作者、卷号、语言和标签，搜索同时覆盖这些字段。

### 候选比较与批量修正

- 分类详情新增当前结果、本地结果和在线结果候选列表，统一显示来源与置信度；选择候选只填写修正框，不会直接改动文件。
- Bangumi、AniList 和 Jikan 的更多候选只在用户点击后按需查询；部分来源失败时保留其他来源的可用结果。
- 同系列批量修正默认关闭，固定按扫描时的规范系列键选组，并在二次确认后以全有或全无方式更新完整预览。
- 详情、候选查询和修正请求携带分类计划版本；扫描或设置变化后到达的旧响应会被拒绝，不能误改新预览。
- 候选仅作为内存中的比较信息，不写入分类报告，也不扩大整理或撤销的文件范围。

### 分类历史与指定批次撤销

- 保留根目录 `classification_report.json` 作为最近报告，同时把校验通过的新报告归档到 `.lightnovel-selector/history`。
- 活动页新增最近批次列表，可查看强类型报告条目、打开原始 JSON、导出副本并撤销指定批次。
- 历史选择使用核心签发的执行编号，不接受任意报告路径；Python 与 WinUI 两侧都限制报告位置并拒绝符号链接、目录联接和越界文件。
- 部分文件已经移动后发生异常时，主报告仍会尽力归档，之前成功的移动不会因后续错误失去撤销入口。
- 已撤销批次原子记录撤销时间和统计；旧版根目录报告继续可用，但不会因无法归档而阻断历史页。
- 历史扫描与界面数量有明确上限，损坏报告单独计数并忽略，不影响其他有效批次。
- 核心在页面已打开后才连接就绪时会自动补刷历史；快速切换批次会正确结束旧加载反馈，导出大报告也不阻塞界面线程。

### 增量扫描与可靠哈希缓存

- 未变化文件可复用快速签名、完整 SHA-256 和本地派生身份，大书库再次扫描不再重复读取每个文件的首尾 2 MiB。
- Windows 使用卷序列号、128 位文件 ID、大小、最后写入时间和 ChangeTime 共同失效缓存；即使只改文件中部并恢复原修改时间，也会重新计算完整哈希。
- 无法取得可靠变更令牌的文件系统自动禁用该文件的缓存复用，继续实时读取，不以性能换取重复检测正确性。
- 快速签名仍只负责缩小候选范围；标记重复前必须比较完整 SHA-256，缓存不会改变原有判定语义。
- 缓存只保存派生书籍身份、查询和哈希，不保存正文片段；文件名回退键经过 SHA-256 处理。
- 缓存采用有界读取、条目/体积上限和原子替换；损坏、超限或写入失败只降低下一次扫描速度，不阻断当前预览。
- 扫描结果和 CLI 会显示复用文件数；手动修正系列名不再重新读文件，并保留作者、卷号、语言和标签。

### 界面自适应与视觉减法

- 降低浅色和深色主题中普通内容面的不透明度与描边亮度，导航 Acrylic 也减少实色覆盖，让窗口材质更容易被感知。
- 四张统计卡合并为一个统计面，分类结果与文件详情合并为一个工作面；重点导入区和底部安全操作区继续保留较明确的高光层级。
- 新增可独立测试的工作区布局策略：宽屏显示 340–380px 详情栏，1024×700 紧凑窗口收起导航与详情栏，同时保留完整结果表格。
- 紧凑窗口不再在选中每一行时自动弹出对话框，改为显式“查看详情”入口，连续搜索和筛选不会被打断。
- 压缩导航栏只保留带说明的本地安全状态点，不再把流程、版本和长文字挤进 64px 区域。
- 外观冒烟覆盖可执行、重复、详情和统计等合成状态，并支持稳定停留后截图，避免只验证空界面。

### 无障碍与键盘工作流

- 为主导航、三个页面、结果筛选以及关键操作补齐 UI Automation 名称、帮助文本、页面地标和标题层级。
- 分类结果、候选系列、历史批次、报告条目与会话日志提供完整朗读摘要，屏幕阅读器可直接获知状态、文件、目标、置信度和来源。
- 新增 `Ctrl+1/2/3` 页面切换、`Ctrl+F` 结果搜索、`Ctrl+S` 保存设置以及 `F6` / `Shift+F6` 主要区域循环。
- 工作台与活动页的 `F5` 按页面作用域分别执行扫描和刷新，现有 `Ctrl+O`、`Ctrl+Enter` 保持不变并可被辅助技术发现。
- 键盘切换页面时跳过装饰性位移动画；系统或应用减少动态效果设置继续控制所有非必要动效。
- 新增纯 C# 焦点循环测试与 XAML 无障碍契约测试，防止后续界面重构丢失快捷键、地标、标题层级或安全范围提示。

### 可扩展元数据提供器

- 新增公开 `MetadataProvider` 接口与不可变 `MetadataProviderRegistry`，社区来源可通过显式注入接入扫描、候选、详情和 Sidecar 服务。
- Bangumi、AniList、Jikan 从 `SeriesResolver` 私有方法迁移到独立模块；分类核心不再包含第三方 API URL、字段映射或来源循环。
- 每个提供器声明稳定 ID、显示名、优先级和缓存版本；缓存键包含有序提供器指纹，记录同时绑定实际命中的来源并在读取时重新校验，更换来源、升级映射或缓存损坏时不会误用旧结果。
- 提供器结果进入核心前统一校验书籍身份、长度、卷号、有限置信度和 HTTPS 链接；异常来源被隔离，后续来源与本地识别仍可继续。
- AniList 与 Jikan 现在也可返回简介、封面、作者和标签等可用书籍详情，在前序来源无可靠结果时安全回退。
- Sidecar 快照附带实际启用来源清单，WinUI 设置页动态显示，不再硬编码单一服务名称。
- 默认不扫描或执行任意本地 Python 插件；新增来源必须显式注册或作为经过审查的内置模块加入。

## v2.0.2 - 2026-07-30

### 性能与稳定性

- 目录发现阶段支持立即取消，并限制单次最多检查 200,000 个目录项，避免大量无关文件让扫描长期无响应。
- 递归扫描对同名文件采用确定性顺序，并忽略解析到所选根目录之外的候选文件。
- 分类结果搜索使用 120ms 短防抖和单次数据源替换，避免大结果集逐行刷新造成输入卡顿。
- 批量整理改为“独占恢复日志、初始报告、最终报告”，执行阶段完整报告固定写入两次，累计写入量不再随文件数量平方增长。
- 每次移动前持久化恢复意图；进程异常退出后会核对源文件、目标文件、原计划和文件状态，再恢复可撤销记录。
- 源和目标同时存在、同时缺失或越过分类根目录时拒绝自动恢复并保留日志，避免猜测文件去向。
- 执行入口同样强制 10,000 项上限，撤销与恢复路径拒绝符号链接、重解析点和非规范路径。
- 撤销记录统一判定为“待撤销”或“已恢复”；两边同时存在、同时缺失或已恢复文件被修改时，在移动任何后续文件前停止。
- 撤销中途被强制结束后可安全续跑；每次移动返回后重新核对源、目标及文件状态。

### 代码清理

- 移除已停用的预加载封面字段、未使用的警告属性和不会由分类核心产生的“冲突”状态。
- 构建时强制校验 Python、C# 与应用清单版本一致，开发版使用独立后缀，避免覆盖或误认稳定版安装器。
- 分类报告时间戳改为包含本地时区偏移，跨时区查看和归档时不再产生时间歧义。
- 开发工具更新到 pytest 9.1.1、Ruff 0.16.0 和最新 Windows SDK BuildTools，并清理新增的静态分析问题。
- 全部 Python 源码建立 Ruff 格式基线，并在 GitHub CI 与正式构建脚本中强制检查。

## v2.0.1 - 2026-07-24

### 安全与稳定性

- 分类报告升级为版本化格式，并记录生成时的分类根目录。
- 撤销开始前统一校验应用标识、报告位置、绝对路径、目录边界和重复路径，异常报告不会触发部分文件移动。
- 保持原目录内旧版报告的撤销兼容；被搬离原目录的报告会提示用户放回后再操作。
- EPUB XML 改用禁止外部实体与 DTD 的安全解析器。
- 远程 JSON 与封面仅允许公开 HTTPS，限制响应和 URL 大小，拒绝协议降级重定向及解析到内网的域名。
- 执行前重新校验源文件状态、符号链接、目标路径与报告位置；扫描后被改写的文件必须重新扫描。
- 每次成功移动后立即原子更新撤销报告。
- 设置、缓存与报告改用随机独占临时文件落盘后原子替换，避免可预测临时路径被占用。
- 新报告记录源文件大小与修改时间；分类后文件被改写、替换为目录或符号链接时拒绝撤销。
- 设置、外观配置、元数据缓存、报告和 Sidecar 请求增加体积与类型边界。
- 本地正文提示与联网查询分离；弱文件名提取出的内容只在本机参与识别。
- GitHub Actions 固定完整提交 SHA，加入 CodeQL、Dependabot、依赖锁定和 Python/.NET 漏洞审计。
- 正式安装器剔除 PDB 与开发期 runtime 配置，避免分发调试符号和本机构建路径。
- 安装器补齐 Python、PyInstaller、defusedxml、.NET、Windows App SDK、WebView2 与 Inno Setup 的原始许可和第三方 Notice。

### 交互与文档

- 整理与撤销确认对话框补充报告保管和路径校验提示。
- 本地封面改为打开详情时按需读取，降低大批量扫描内存占用。
- 重复指纹计算支持分块取消，单次扫描限制为 10,000 个受支持文件，避免超大列表拖垮界面。
- 详情请求增加防抖与串行控制，慢网络不再触发错误的核心重连。
- 活动页限制超大报告的明细载入量并显示截断状态，完整报告和撤销不受影响。
- 主程序启用单实例激活，重复启动只唤回已有窗口，避免两个进程同时整理同一目录。
- README 与安全说明补充下载校验、联网范围、分类报告隐私和未签名安装器边界。

## v2.0.0 - 2026-07-24

- 桌面界面全面切换为 WinUI 3 原生工作台。
- 改用单 EXE 安装器交付，并保留 Python Sidecar 分类核心。
- 加入 Acrylic、Mica、实色材质，自适应深浅色和减少动态效果。
- 完善预览筛选、手动修正、报告明细、撤销、重连与批处理安全保护。
- 项目采用 MIT License，并补齐中文贡献、安全和社区协作文件。

完整说明见 [v2.0.0 更新说明](docs/releases/v2.0.0.md)。
