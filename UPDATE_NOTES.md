# LightNovelSelector v2.0.0 更新说明

发布日期：2026-07-20

## 更新重点

v2.0.0 是一次完整架构与界面重构。分类、文件移动、报告和撤销仍由 Python 执行；旧 Tk 单体界面替换为 pywebview + WebView2 桌面界面，原命令行入口和核心功能保持兼容。

## 架构重构

- 将原单文件程序拆分为常量、模型、存储、解析、文件处理、元数据、分类、应用服务、CLI 和桌面桥接模块。
- 新增线程安全的 `ApplicationService`，统一管理扫描、执行、撤销、进度、日志和预览版本。
- 新增受控 pywebview API，界面不能直接访问文件系统。
- 保留 `lightnovel_classifier.py` 兼容入口和原有 CLI 参数。
- 根目录测试固定收集 `tests`，不再误收集实验目录中的同名测试。

## 全新界面

- 使用中性石墨深色工作台，建立青色识别、琥珀执行、绿色成功、紫色系列和红色错误的语义色体系。
- 新增左侧安全工作流、来源目录、统计区、分类队列、详情面板和底部操作栏。
- 分类队列支持搜索、系列筛选和状态筛选。
- 设置改为原生语义开关，选中反馈不再显示容易混淆的叉号。
- 新增手动修正、创建目录、执行确认、撤销确认、报告查看和日志抽屉。
- 窄窗口下自动收敛为图标导航，所有图标操作均保留名称或 tooltip。
- 支持 Windows “减少动态效果”设置。

## 动效与操作反馈

- 按钮按压反馈限制为 130ms。
- 弹窗、Toast 和抽屉使用 140 至 240ms 的强非线性缓动。
- Toast 退出快于进入，进出方向保持一致。
- 进度条只使用 GPU 友好的 `transform`。
- 高频表格选择、筛选和键盘快捷键保持即时，不增加延迟动画。
- 扫描、执行和撤销期间统一锁定冲突操作。
- 关闭窗口时若正在移动或恢复文件，会阻止退出并给出明确提示。

## 文件与网络安全

- 重复文件继续使用快速签名筛选和完整 SHA-256 确认。
- 报告不可写时不会开始移动文件。
- 移动中途失败仍会保留可撤销的部分报告。
- 手动修正从界面私有逻辑提升为可测试的核心函数。
- 重复项经过手动修正后可安全转为可执行计划，并清除重复标记。
- 在线封面限制为 8 MiB，非图片响应不会交给界面显示。
- 并发扫描请求会被拒绝，不会额外清空现有预览版本。

## Windows 运行与打包

- 新 `run.bat` 会自动创建 `.venv` 并安装固定版本的 pywebview。
- Python 或用户名路径包含空格时仍使用完整引号执行。
- 系统重装后损坏的虚拟环境会移入 `archive_old_code`，再自动重建。
- 新 `build_exe.bat` 会打包 WebView 静态资源并从 Python 常量读取版本。
- v2.0.0 单文件 EXE 约 14.6 MB。

## 测试结果

- 42 项 Python 自动测试全部通过。
- Python 编译检查通过。
- ruff 静态检查通过。
- vulture 高置信度无用代码检查无发现。
- JavaScript 语法检查通过。
- 1440×900、1280×800、1080×700 界面截图检查通过。
- Python 3.12 构建环境与 Python 3.13 源码运行环境均通过启动冒烟。
- PyInstaller 单文件 EXE 启动冒烟通过。

## 发布资产

```text
LightNovelSelector-v2.0.0-构建时间.exe
```

升级后直接运行新版即可。旧设置文件会继续读取，并自动补充最近目录字段。
