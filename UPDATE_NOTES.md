# LightNovelSelector v2.0.0 WinUI 原生界面更新说明

更新日期：2026-07-20

适用范围：WinUI 3 原生界面开发分支

## 更新重点

本次更新保留 Python 分类核心和现有文件安全逻辑，新增完整的 WinUI 3 原生桌面前端。界面、任务状态和文件操作通过有版本的 JSON Lines 协议解耦，下载版打包为自包含 Windows x64 应用。

## 原生架构

- 新增 `native/LightNovelSelector.WinUI` 原生项目。
- 使用 WinUI 3、Windows App SDK、XAML 和 Windows Composition。
- 新增 Python Sidecar 协议，支持请求编号、并发响应关联、结构化错误和超时。
- 标准输出只承载协议，运行诊断写入标准错误。
- 启动时执行协议版本握手，不兼容时给出明确错误。
- 关闭界面时优雅请求 Sidecar 退出，异常情况下终止整个子进程树。
- C# 只负责表现层，不直接移动、删除、重命名或写分类报告。

## 全新界面

- 使用 Windows 原生 Mica 窗口背景和 Acrylic 临时通知层。
- 新增原生标题栏、左侧导航、导入区、四项统计、虚拟化分类列表、详情面板和安全操作栏。
- 新增活动与报告页，可查看最近报告和结构化日志。
- 新增设置页，支持联网识别、递归扫描、自动重命名、自定义规则、主题和减少动态效果。
- 支持目录选择和批量拖放导入。
- 支持结果详情、封面、来源、简介、目标路径与外部条目。
- 支持手动修正分类结果。
- 复选框选中使用对勾，避免与错误状态混淆。
- 窄窗口自动收起导航并切换紧凑详情方式。
- 浅色、深色、系统主题和高对比度均使用语义设计令牌。

## 动效与操作反馈

- 指针按压使用 110 至 180 毫秒的轻微缩放反馈。
- 卡片进入使用 240 至 280 毫秒强 ease-out 与短距离位移。
- Toast 进入 220 毫秒、退出 140 毫秒，退出快于进入。
- 动效只修改 Composition 的透明度、缩放和位移，不触发布局动画。
- 高频选择和键盘操作不增加动画等待。
- 自动尊重 Windows 动画辅助功能，也支持应用内减少动态效果开关。
- 扫描、执行、撤销期间锁定冲突操作，并显示真实进度和取消状态。
- 文件移动和撤销期间阻止误关窗口。

## Python 核心与安全

- WinUI 已接通扫描、预览、手动修正、执行、取消、报告、撤销和设置完整流程。
- 重复文件继续使用完整 SHA-256 内容确认，避免相同头尾但中间不同的误判。
- 报告不可写时不会开始移动。
- 每次成功移动后立即原子更新报告，后续失败仍可撤销已完成部分。
- 设置保存失败只显示警告，不阻断扫描。
- 重复项、错误项和无需移动项使用文字、图标和颜色共同表达。
- 在线封面继续执行大小限制和格式验证。

## Windows 构建与分发

- 新增 `run_winui.bat`，自动查找有效 Python 与 .NET 10 SDK 并启动调试版。
- 新增 `build_winui.bat`，统一执行依赖、图标、Sidecar、协议、发布、冒烟和压缩。
- 新增 `lightnovel_sidecar.py` 作为 PyInstaller 最小入口。
- 发布包优先使用同目录的 `LightNovelSelector.Sidecar.exe`，不会误用系统 Python。
- WinUI 采用自包含 x64 发布，下载者无需安装 Python、.NET SDK 或 Windows App SDK Runtime。
- 新增确定性图标生成器，替换 WinUI 模板占位图。
- EXE、任务栏、磁贴、商店和启动图资源使用统一书本加对勾标识。
- EXE 文件版本、产品名和中文说明与 Python `v2.0.0` 对齐。

## 已验证结果

- 45 项 Python 自动测试通过。
- Python 编译、ruff 和高置信度无用代码检查通过。
- Sidecar 源码模式和 PyInstaller 冻结模式协议测试通过。
- WinUI Debug、Release 构建无警告、无错误。
- WinUI 冒烟启动和自动退出通过。
- 真实测试目录完成扫描、重复检测、手动修正、移动、报告和完整撤销。
- 150% DPI 下 1440×900 窗口布局通过检查。
- 窄窗口响应式布局、设置页、活动页、Toast、进度和确认对话框通过检查。
- 发布版确认启动包内 Sidecar，关闭主窗口后父子进程均退出。
- 发布版 EXE 已提取验证正式应用图标。

## 发布产物

```text
LightNovelSelector-v2.0.0-win-x64-构建时间\
LightNovelSelector-v2.0.0-win-x64-构建时间.zip
```

本次实测自包含目录约 230.45 MB，ZIP 约 94.28 MB。该分支只用于原生界面评审与后续合并，不创建 Release，也不直接改动 `main`。
