# LightNovelSelector v2.0.2 更新说明

发布日期：2026-07-30

## 本轮重点

`v2.0.2` 是 WinUI 3 原生版的批量性能与异常恢复更新。分类规则、报告版本和
Sidecar 协议保持兼容，重点解决大批量整理反复重写完整报告的问题，并补齐进程
强制中断、撤销续跑和歧义文件状态的安全边界。

## 批量报告与异常恢复

- 整理开始时独占创建恢复日志，已有未恢复现场会阻止新任务覆盖原报告。
- 完整报告只在执行开始和结束或中断边界汇总，不再每移动一个文件就重新序列化全部计划。
- 每次移动前向 JSON Lines 恢复日志追加源路径和实际目标路径，并立即 `fsync`。
- 进程被强制结束后，软件会核对执行编号、分类根目录、原计划、文件大小和修改时间，再重建可撤销记录。
- 恢复日志的版本、执行编号、路径和文件状态均严格校验；布尔值、浮点版本号及被篡改记录不会被接受。
- 源与目标同时存在、同时缺失、路径越界、变成链接或目录时拒绝猜测，并保留现场供人工检查。

## 撤销安全

- 每条撤销记录统一判定为“待撤销”或“已恢复”，其他状态均视为歧义。
- 撤销开始前先验证整批记录；后续项目存在冲突时，不会先恢复前面的文件。
- 已恢复文件必须仍与报告中的大小和修改时间一致，避免把无关文件误判为成功撤销。
- 撤销中途被强制结束后可以再次执行：已恢复项安全跳过，其余项目继续恢复。
- 每次文件移动返回后重新核对源和目标状态，移动函数未真正完成时不会计入成功数量。
- 跨卷移动留下源和目标两个副本时停止自动操作，不会静默跳过或删除任意一边。

## 扫描、筛选与代码质量

- 目录发现阶段支持立即取消，并限制单次最多检查 200,000 个目录项和 10,000 个受支持文件。
- 递归扫描顺序稳定，解析到所选分类根目录之外的候选文件会被忽略。
- 分类结果搜索使用短防抖和单次数据源替换，减少大结果集筛选时的界面刷新。
- Python 开发工具更新到 pytest 9.1.1 与 Ruff 0.16.0。
- 全部 Python 源码建立统一 Ruff 格式基线，GitHub CI 和正式构建都会阻止格式漂移。
- Python、WinUI、应用清单和安装器版本在构建时统一校验，避免生成版本不一致的安装包。

## 验证结果

- Python：114 项测试和 9 个参数化子测试通过。
- C#：25 项测试通过。
- WinUI 3 Debug/Release x64：0 警告、0 错误。
- Ruff lint/format、Mypy、Bandit、Vulture、pip-audit、NuGet audit 和 `git diff --check` 通过。
- Sidecar 冻结、协议验证、源码启动、自包含发布和安装器冒烟测试通过。
- GitHub Windows CI 与 Python/C# CodeQL 通过。

## 兼容性

- 支持 Windows 10 1809 或更高版本以及 Windows 11。
- 正式安装器继续自带 Python Sidecar、.NET 和 Windows App SDK，不要求用户另装运行环境。
- 分类报告保持 schema v2，Sidecar 协议保持兼容。
- 旧版 `classification_report.json` 仍可在原分类目录中撤销。

## 已知限制

- 安装器目前没有商业代码签名证书，Windows 可能显示“未知发布者”。请只从本仓库 Release 下载并核对 SHA-256。
- 当前正式安装器主要面向 Windows x64。
- 在线元数据依赖第三方服务；服务不可用时会回退本地规则，不影响本地文件整理。

`v2.0.1` 的历史说明见 [docs/releases/v2.0.1.md](docs/releases/v2.0.1.md)。
