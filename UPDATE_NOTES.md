# LightNovelSelector v2.0.1 更新说明

发布日期：2026-07-24

## 本轮重点

`v2.0.1` 是 WinUI 3 原生版的发布前加固更新。分类规则和 Sidecar 协议保持兼容，重点收紧文件、网络、配置和供应链边界，并降低批量扫描与详情加载的资源占用。

## 文件与撤销安全

- 分类执行前统一验证分类根目录、源文件、目标目录、目标文件和报告位置，拒绝跨根目录计划。
- 扫描后源文件的大小或修改时间发生变化时停止执行，提示重新扫描，避免移动与预览不一致的内容。
- 扫描与执行都跳过符号链接，防止无意处理所选目录之外的文件。
- 每次移动成功后立即原子更新 `classification_report.json`，后续操作失败时仍可撤销已完成项。
- 设置、缓存与报告使用随机独占临时文件，完整落盘后再原子替换正式文件。
- 新报告记录文件大小与修改时间；分类后目标被编辑、替换为目录或符号链接时拒绝撤销。
- 报告读取增加 64 MiB 上限，并继续兼容保留在原目录中的旧版报告。
- Windows 保留设备名会自动转换为安全目录名，避免 `CON`、`NUL`、`COM1` 等名称导致创建失败。

## 网络与内容解析

- EPUB XML 改用 `defusedxml`，禁止外部实体与 DTD，损坏或恶意元数据会安全降级。
- 远程 JSON 与封面只允许公开 HTTPS 地址，拒绝登录信息、本机/内部地址、解析到内网的域名和 HTTPS 到 HTTP 的降级重定向。
- JSON、封面、URL、摘要、设置、缓存和 Sidecar 单条请求均有明确体积上限。
- 在线封面在进入 WinUI 解码前校验图片签名，并限制解码宽度，降低异常大图造成的内存压力。
- 条目链接只允许公开 HTTPS；不安全链接不会交给系统浏览器。
- 弱文件名会继续使用本地正文提示识别，但该提示不再作为联网查询发送。
- 第三方元数据和本地缓存统一校验字段类型、长度与有限置信度，异常数据只会降级当前条目。
- 活动页最多载入前 1000 条净化后的报告明细，并显示总数；完整 JSON 与撤销能力不受影响。

## 性能与交互稳定性

- 扫描阶段不再把每本 EPUB/ZIP 的封面长期保存在内存中，改为打开详情时按需读取。
- 详情选择增加 140ms 防抖，并确保同一时间只有一个远程详情请求。
- 详情加载期间暂停健康轮询，慢网络不会再被误判为 Python 核心失联。
- 快速切换选择时，旧详情不会覆盖新选择；页面关闭时正确释放详情和 Toast 取消资源。
- 打开文件位置改用 Windows Storage/Launcher API，避免命令行参数解析和可执行文件搜索歧义。
- 源码模式可识别 Python 3.10–3.19、项目虚拟环境和系统 Python Launcher，不再绑定单个重装前路径。
- 主程序启用单实例激活；重复启动只唤回已有窗口，避免两个分类核心同时操作同一目录。
- 重复指纹计算按块响应取消请求；单次扫描最多处理 10,000 个受支持文件，超出时提示分目录整理。

## 依赖与 GitHub 安全

- Python 运行依赖、开发依赖和 NuGet 依赖均固定版本；NuGet 提交锁文件并启用漏洞审计。
- GitHub Actions 固定到完整提交 SHA，避免可移动标签带来的供应链风险。
- 新增 CodeQL，对 Python 与 C# 定期分析。
- 新增 Dependabot，按周检查 GitHub Actions、pip 和两个 NuGet 项目。
- Windows CI 新增 Mypy、Bandit、pip-audit、`pip check` 和锁定模式还原。
- 新增中文发布检查清单，覆盖密钥、隐私、文件安全、构建、安装、Defender、签名和 Release 校验。
- 正式安装器不再包含 PDB 或开发期 runtime 配置，并在打包前自动复查发布暂存区。

## 验证结果

- Python：94 项测试和 6 个参数化子测试通过。
- C#：25 项测试通过。
- WinUI 3 Release x64：0 警告、0 错误。
- Ruff、Mypy、Bandit、Vulture、pip-audit、NuGet audit、actionlint 与 `git diff --check` 通过。
- Git 历史密钥扫描无发现。

## 已知限制

- 安装器目前没有商业代码签名证书，Windows 可能显示“未知发布者”。请只从本仓库 Release 下载并核对 SHA-256。
- 在线元数据依赖第三方服务可用性；服务不可用时会回退本地规则，不影响本地文件整理。

`v2.0.0` 的历史说明见 [docs/releases/v2.0.0.md](docs/releases/v2.0.0.md)。
